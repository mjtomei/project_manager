"""Markdown format primitives for the adversarial-review walker.

Parsers (`md_parser`) and writers (`md_writer`) for the markdown surfaces
the walker reads and mutates: response blocks, interaction logs, audit
docs, response docs, `STATE.md`, `UI_FOCUS.md`, and `NOTES.md`.
"""
