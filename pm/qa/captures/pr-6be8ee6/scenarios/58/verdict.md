# Scenario 58: Review-loop INPUT_REQUIRED gate and duplicate-verifier guard

INPUT_REQUIRED

refiner rejected: The first two triples require launching the real bug-fix review loop (`pm pr review --review-loop`), which spawns a Claude reviewer agent in a tmux pane and waits for it to emit a verdict. The INPUT_REQUIRED "gate" for missing captures is not a deterministic code check — it's a prompt instruction in `_bug_fix_review_block` (pm_core/bug_fix_prompts.py:76-79) asking the reviewer LLM to surface that verdict. Driving this from the user surface needs a real reviewer session (multi-minute, billable, non-deterministic), and there is no fake-reviewer harness in this PR (FakeClaudeSession is noted as a future PR-abcf70f). The third triple is even more dependent on real agents: observing the `_last_scenario_hook_ts` guard in `_poll_tmux_verdicts` (pm_core/qa_loop.py:1965-1985) requires an active `pm qa run` with a real scenario pane reaching PASS, a real verifier session emitting FLAGGED, and a follow-up keystroke round-trip — there is no user-facing way to inject those states without monkeypatching the loop internals. No external dependency or fake harness exists in this run to substitute for the live Claude sessions.
